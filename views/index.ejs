<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/css/style.css">
    <title>Document</title>
</head>
<body>
    <header>
        <nav class="main-navigation">
            <a href="/">
                <svg class="icon site-logo">
                    <use href="../icons-svg/icons.svg#logo-small-red"/>
                </svg>
            </a>
            <a href="">
                <svg >
                    <use href="../icons-svg/icons.svg#navigation-favorite" />
                </svg>
            </a>
            <a href="">
                <svg >
                    <use href="../icons-svg/icons.svg#navigation-hamburger" />
                </svg>
            </a>
            <a href="">
                <svg >
                    <use href="../icons-svg/icons.svg#navigation-favorited" />
                </svg>
            </a>
            <a href="">
                <svg >
                    <use href="../icons-svg/icons.svg#navigation-search" />
                </svg>
            </a>
            <a href="">
                <svg >
                    <use href="../icons-svg/icons.svg#navigation-podcasts" />
                </svg>
            </a>
        </nav>
    </header>   
    <main>
        <% if (data && data.data.length) { %>
            <% data.data.forEach(collection => { %>
                <!-- Horizontal podcast item -->
                <a id="<%= collection.attributes.slug %>" href="/collection/<%= collection.attributes.slug %>">
                    <div>
                        <picture>
                            <img srcset="<%= imageFiles[mainVisuals[collection.relationships.mainVisual.data.id]]%>" alt="" width="100" height="100">
                        </picture>
                        <!-- Hier komt de image cover van de podcast -->
                    </div>
                    <div>
                        <p><%= collection.attributes.title %></p>
                    </div>
                </a>
            <% }); %>
        <% } else { %>
            <p class="error">Error loading podcasts</p>
        <% } %>               
        

    </main>
        
</body>
</html>

